import { UnidadesLikesPipe } from './unidades-likes.pipe';

describe('UnidadesLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new UnidadesLikesPipe();
    expect(pipe).toBeTruthy();
  });
});
